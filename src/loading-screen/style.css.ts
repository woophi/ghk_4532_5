import { style } from '@vanilla-extract/css';

export const loadingPageLayout = style({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100vh',
  padding: '0 1rem',
});
